package com.veyora.crm.constant;

/**
 * Mirrors the roles that guard the hotel extranet actions in tf-main
 * (see HotelAction role gating).
 */
public enum RoleType {
    ADMIN,
    SUPERVISOR,
    PRODUCT,
    BUSINESS_MANAGER,
    FINANCE,
    HOTELIER,
    TOUR_OPERATOR,
    EXPERT,
    CALLCENTER
}
