spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/crm
    username: crm_app
    password: crm_app_local
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    root: INFO
    com.veyora: DEBUG
